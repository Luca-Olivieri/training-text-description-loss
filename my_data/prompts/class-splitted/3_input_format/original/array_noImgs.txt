I will give you the ground truth and the prediction masks both in form of NumPy arrays of shape (H, W), in which each coordinate corresponds to the class identifier of the class assigned to that pixel.
The first NumPy array encodes the ground truth, the second one encodes the prediction.
